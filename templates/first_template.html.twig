<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% block body %}
            {{ form_start(form) }}
                {{ form_row(form.name) }}
                {{ form_row(form.age) }}
                <button class="btn">Submit</button>
            {{ form_end(form) }}
        {% endblock %}
    </body>
</html>
